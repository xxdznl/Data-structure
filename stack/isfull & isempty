typedef struct node *stack;
struct node{
   elementtype data[maxsize];//stack length
   int top;// stack top
}
int isfull(stack ptrs)
{
  if(ptrs->top == maxsize-1)//isempty：if（ptrs->top == -1）
  return 1;//1为满（空）
  else 
  return 0;//0为非满（非空）
}
