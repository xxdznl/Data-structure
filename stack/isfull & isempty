typedef struct node *stack;
struct node{
   elementtype data[maxsize];//stack length
   int top;// stack top
}
int isfull(stack ptrs)
{
  return if(ptrs->top == maxsize-1)//isempty：return if(ptrs->top == -1);
}
